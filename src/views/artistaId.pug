<!DOCTYPE html>
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title= titleArt
        include partials/_head.pug
        
    body
        include partials/_header.pug
        .bannerArtistas(style=`background: ${url};background-attachment: fixed; background-repeat: no-repeat; background-size: cover;`)
        section
            include partials/_title.pug
            .contenedor-perfil
                .perfil-contenedor
                    img(src=`${urlPerfil}`, alt="Foto de Perfil")
                .curriculum
                    p=parrafoUno 
                    p=parrafoDos
                    p=parrafoTres
                    p=parrafoCuatro
                    p=parrafoCinco
            .contenedor-galeria
                each artista in general
                    .contenedor-galeria-foto
                        img(src=`${artista.obra}`, alt="")
                        p=artista.titulo
                        p=artista.tamano
                        p=artista.tecnica
                        p=artista.ano
                        a(href=`${artista.enlace}`) Ver más

                    

            include partials/_footer.pug
